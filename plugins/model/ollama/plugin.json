{
  "name": "ollama",
  "version": "0.2.0",
  "layer": "model",
  "interface": "IModelAdapter",
  "entrypoint": "dist/index.js",
  "description": "Default LLM adapter via Ollama (gemma4:e4b / gemma4:e2b). Falls back to a deterministic offline indexer if Ollama is unreachable so the pipeline still produces output.",
  "config_schema": {
    "type": "object",
    "properties": {
      "model": { "type": "string" },
      "host": { "type": "string" },
      "vision_model": { "type": "string" }
    }
  }
}
