{
  "name": "markdown",
  "version": "0.2.0",
  "layer": "export",
  "interface": "IExport",
  "entrypoint": "dist/index.js",
  "description": "Default export — mirrors indexed wiki pages to a stable, human/agent-readable .md tree for Obsidian, Logseq, portable Markdown tools, and agents.",
  "config_schema": {
    "type": "object",
    "properties": {
      "path": { "type": "string" },
      "profile": {
        "type": "string",
        "enum": ["obsidian", "logseq", "portable"],
        "default": "obsidian"
      },
      "link_style": {
        "type": "string",
        "enum": ["wikilink", "markdown"],
        "description": "Override profile link style. Defaults to wikilink for Obsidian/Logseq and markdown for portable exports."
      }
    }
  }
}
