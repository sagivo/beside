{
  "name": "@beside/website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Beside marketing site — AI memory & context layer for agents.",
  "scripts": {
    "dev": "vite",
    "build": "vite build --outDir dist/client && vite build --ssr src/entry-server.tsx --outDir dist/server && node scripts/prerender.mjs",
    "preview": "vite preview --outDir dist/client",
    "serve": "node scripts/serve.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@mdx-js/rollup": "^3.1.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "remark-gfm": "^4.0.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
